/*
package com.stratio.examplesTest


import org.scalatest.FlatSpec

/**
  * Ejercicio practico metodologia TDD:
  *  Partiendo de las historias de usuario y/o casos de uso con sus criterios de aceptacion,
  *  se definen los siguientes test, y una API (metodos vacios) - OrderPizza.scala.
  *  Se trata de ir desarrollando los test y ir refactorizando los metodos de la API para que
  *  puedan ir pasando los tests.
  *  Esto es un proceso iterativo por test (o grupo de tests)
  */
class SuiteTest4Exercise extends FlatSpec{

  val greenOlives = Topping("green olives",1.3)
  val onions = Topping("onions",3)

  "A New Pizza" should "have zero toppings" is (pending)


  "A Pizza" should "have size equal 1 after to add a new topping" is (pending)


  it should "have size equal 0 after to remove a topping" is (pending)


  it should "not remove a topping that doesn't exist" is (pending)


  "A Empty Pizza" should "fail when one topping is removed" is (pending)


  "A Pizza" should "return the price of all its toppings" is (pending)
}
*/